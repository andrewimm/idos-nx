//! DriverIO handles async communication between the kernel and drivers. These
//! may be device drivers, filesystems, or other services accessed by handles.

pub fn driver_io_task() -> ! {

    loop {
        
    }
}
