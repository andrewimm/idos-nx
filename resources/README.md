# resources — Boot Disk Static Files

Static files copied onto the IDOS boot disk image at build time.

- **`DRIVERS.CFG`** — Driver and service configuration loaded by the kernel after bootstrap (hardware drivers, PCI device mappings, filesystem mounts, graphics, networking, console)
- **`ter-i14n.psf`** — Terminus 14px console font (PC Screen Font format)
